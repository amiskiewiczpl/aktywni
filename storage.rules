rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /banners/{allPaths=**} {
      allow read: if true;
      allow write: if request.time < timestamp.date(2099,1,1);
    }
    match /{allPaths=**} {
      allow read: if true;
    }
  }
}
