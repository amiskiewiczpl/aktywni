{
  "name": "aktywni",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173",
    "emu:login": "npx firebase login",
    "emu:init": "npx firebase init emulators",
    "emu:start": "npx firebase emulators:start --only firestore,auth,storage"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "firebase-tools": "^13.9.0"
  },
  "dependencies": {
    "firebase": "^10.12.0"
  }
}
